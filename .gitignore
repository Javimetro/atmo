# Arduino IDE build output
build/
*.hex
*.bin

# Old PlatformIO scaffold -- dropped from this project, but something
# (an editor window with the PlatformIO extension) keeps regenerating
# these locally. Ignored so they never get committed again.
.vscode/
platformio.ini
.pio/
include/
lib/
test/
